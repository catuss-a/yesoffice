- if devise_mapping.omniauthable?
  %hr
  - resource_class.omniauth_providers.each do |provider|
    %center= link_to "Se connecter avec Google", omniauth_authorize_path(resource_name, provider), class: 'btn btn-info btn-large'
    %br/
